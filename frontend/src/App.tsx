export function App() {
  return (
    <main className="min-h-screen bg-paper p-8 font-display text-ink">
      <h1 className="text-4xl font-semibold">Read the question.</h1>
      <p className="mt-2 font-body italic text-ink-muted">
        Then ask yours. Archive / Editorial tokens wired.
      </p>
      <button className="mt-6 rounded-md border border-claret px-4 py-2 font-ui uppercase tracking-widest text-claret">
        Sign in
      </button>
    </main>
  );
}
