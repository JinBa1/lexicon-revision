# Ingestion worker image: API deps + MinerU on CPU torch.
# requirements-ingest.txt is the GPU/local-dev variant (CUDA torch); this
# file must stay CPU-only -- Fargate has no GPU and prod embeds via Voyage.
# mineru[pipeline], not [all]: the worker only invokes the pipeline backend,
# and [all] drags in the vllm VLM stack (>1.3GB of CUDA components).
-r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0+cpu
torchvision==0.24.0+cpu
mineru[pipeline]==3.0.8
